# Project Scope

`vhttp` is a from-scratch HTTP/1.1 server project focused on learning and demonstrating transport, parsing, routing, connection management, security hardening, concurrency, and measurable performance. It intentionally avoids HTTP server frameworks while still using normal C++20 and operating-system facilities.
